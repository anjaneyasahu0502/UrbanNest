{
  "name": "urbannest-frontend",
  "version": "2.0.0",
  "description": "UrbanNest frontend — static assets + dev proxy to backend",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
